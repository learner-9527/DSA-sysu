#include "test.h"
#include <windows.h>
#include <iostream>
#include <vector>
#include <string>
using namespace std;

int main()
{
    vector<int> a(10);
    cin >> a[0];
    cout << a[0] << endl;
    system("pause");
    return 0;
}